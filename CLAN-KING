local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))() 

local Window = Rayfield:CreateWindow({
   Name = "MUSCLE KING ON TOP",
   Icon = 0,
   LoadingTitle = "KING ON TOP",
   LoadingSubtitle = "by 1_F0",
   ShowText = "Rayfield",
   Theme = "Default",
   ToggleUIKeybind = "K",
   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false,

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil,
      FileName = "King on top hub"
   },

   Discord = {
      Enabled = true,
      Invite = "https://discord.gg/ayt6qxRca",
      RememberJoins = true
   },

   KeySystem = true,
   KeySettings = {
      Title = "Untitled",
      Subtitle = "KINGONTOP System",
      Note = "Key en el grupo de discord clan",
      FileName = "Key",
      SaveKey = true,
      GrabKeyFromSite = true,
      Key = {"KINGONTOP"}
   }
})

-- Pestaña Main
local MainTab = Window:CreateTab("🏠 Main", nil)
local MainSection = MainTab:CreateSection("Auto Farm")

local AutoWeight = false
local AutoRebirth = false
local AutoRebirth1000 = false

-- Option 1: AUTO WEIGHT
MainTab:CreateToggle({
    Name = "🏋️ AUTO WEIGHT",
    CurrentValue = false,
    Flag = "AutoWeight",

    Callback = function(Value)
        AutoWeight = Value
        
        task.spawn(function()
            local player = game:GetService("Players").LocalPlayer
            
            while AutoWeight do
                local character = player.Character
                if character and character:FindFirstChildOfClass("Humanoid") then
                    -- Equipar la pesa
                    local weightTool = player.Backpack:FindFirstChild("Weight") or character:FindFirstChild("Weight")
                    if weightTool then
                        if weightTool.Parent ~= character then
                            character.Humanoid:EquipTool(weightTool)
                        end
                        
                        -- Simular la activación/clic del tool en el personaje
                        weightTool:Activate()
                    end
                    
                    -- Por si el juego requiere disparar su evento específico
                    local muscleEvent = player:FindFirstChild("muscleEvent")
                    if muscleEvent then
                        muscleEvent:FireServer("rep")
                    end
                end
                task.wait(0.05)
            end
        end)
    end,
})

-- Option 2: AUTO REBIRTH
MainTab:CreateToggle({
    Name = "🔄 AUTO REBIRTH",
    CurrentValue = false,
    Flag = "AutoRebirth",

    Callback = function(Value)
        AutoRebirth = Value
        
        task.spawn(function()
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            
            while AutoRebirth do
                local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
                if rEvents and rEvents:FindFirstChild("rebirthRemote") then
                    local rebirthRemote = rEvents.rebirthRemote
                    if rebirthRemote:IsA("RemoteFunction") then
                        rebirthRemote:InvokeServer("rebirthRequest")
                    elseif rebirthRemote:IsA("RemoteEvent") then
                        rebirthRemote:FireServer("rebirthRequest")
                    end
                end
                task.wait(0.5)
            end
        end)
    end,
}) 

-- Option 3: AUTO RENACIMIENTO x1000
MainTab:CreateToggle({
    Name = "⚡ Auto Renacimiento x1000",
    CurrentValue = false,
    Flag = "AutoRebirth1000",

    Callback = function(Value)
        AutoRebirth1000 = Value

        task.spawn(function()
            while AutoRebirth1000 do
                local Event = game:GetService("ReplicatedStorage").rEvents.rebirthRemote
                Event:InvokeServer(
                    "massRebirthRequest",
                    1000
                )
                task.wait(0.5)
            end
        end)
    end,
})

-- Pestaña Pet
local PetTab = Window:CreateTab("🐶 Pet", nil)
local PetSection = PetTab:CreateSection("Crystals & Pets")

local AutoCrystal = false

PetTab:CreateToggle({
    Name = "💎 Auto Cristal",
    CurrentValue = false,
    Flag = "AutoCrystal",

    Callback = function(Value)
        AutoCrystal = Value

        task.spawn(function()
            while AutoCrystal do
                local Event = game:GetService("ReplicatedStorage").rEvents.openCrystalRemote
                Event:InvokeServer(
                    "openCrystalBatch",
                    "Insufficient Masters Crystal",
                    10
                )
                task.wait(0.1)
            end
        end)
    end,
})
